{
  "name": "future-account",
  "private": true,
  "description": "Syna Co — Arabic ERP monorepo",
  "scripts": {
    "dev:api": "cd backend && php artisan serve",
    "dev:web": "cd frontend && npm run dev",
    "migrate": "cd backend && php artisan migrate --seed",
    "build": "cd frontend && npm run build"
  }
}
